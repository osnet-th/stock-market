package com.thlee.stock.market.stockmarket.user.domain.model;

/**
 * 사용자 권한
 */
public enum UserRole {
    /**
     * 일반 사용자 권한
     */
    USER,

    /**
     * 가입 중인 사용자 권한
     */
    SIGNING_USER
}